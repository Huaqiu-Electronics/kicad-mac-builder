{
  "_comment": [
    "Single source of truth for the pinned hq-edge edge-headless runtime that gets",
    "bundled into KiCad.app/Contents/Resources/edge-headless on Apple Silicon.",
    "",
    "Read at CMake configure time by kicad-mac-builder/edge-headless.cmake, so editing",
    "this file changes what the next `build.py` run downloads (nothing is ever fetched",
    "from a floating 'latest' URL).",
    "",
    "Update it with:",
    "    kicad-mac-builder/bin/pin-edge-headless.py --version <tag> --local <zip>",
    "which computes the SHA256 for you, or hand-edit and validate with",
    "    kicad-mac-builder/bin/pin-edge-headless.py --print / --check <zip>",
    "",
    "A single build can still override any field with",
    "    build.py --edge-headless-version ... --edge-headless-sha256 ... --edge-headless-url ..."
  ],
  "edgeHeadless": {
    "enable": true,
    "version": "0.1.9",
    "asset": "edge-headless-darwin-arm64.zip",
    "sha256": "5ae192831d3ad5b3183fe5103401235f2b50692e0d97502ee7ede37c1948bda4",
    "repoUrl": "https://github.com/Huaqiu-Electronics/edge-headless/releases/download",
    "url": "",
    "notes": "darwin-arm64, built by 'pnpm dist:edge-headless:zip:darwin-arm64' in hq-edge (single edge-headless/ root, no __MACOSX, no dsh/.dsh)."
  }
}
